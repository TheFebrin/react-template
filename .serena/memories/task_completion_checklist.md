# Task Completion Checklist
When finishing coding work in this project:
1. Run `npm run lint` and fix reported issues.
2. Run `npm run build` to verify TypeScript + production build succeed.
3. If behavior/UI changed, run `npm run dev` and verify the affected flow manually.
4. Review `git diff` for unintended changes before committing.